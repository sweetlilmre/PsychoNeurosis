# The census table: every script in both repos, and where it is in its
# retirement. Read by kit/tools/census.py.
#
# The originals in tools/ and in the sibling repo are FROZEN and keep
# working. The toolkit is built beside them by copy and adjust. Once a
# toolkit tool supersedes an original, the original is archived -- so
# "never refactor the originals" is transitional, not permanent.
#
# No machine path appears here. The sibling repo's location is passed on
# the command line or set in the untracked local config.
[script."align.py"]
state = "carry"
reason = "the coverage question -- which bytes of an original do not line up against a rebuild, allowed-difference rule passed in"


[script."artefact.py"]
state = "carry"
reason = "guards the byte-identical rebuilds"


[script."asmaudit.py"]
state = "archived"
successor = "kit/tools/pascal/asmaudit.py"
reason = "checks two of the three transcription rules mechanically -- a comment on every assembler line, and the equivalent Pascal above it. VERIFIED: output identical bar the provenance line every kit program prints, 12 units meeting the rule and 43 not. Its source directory was a constant and is now layout.src"


[script."asmcheck.py"]
state = "archived"
successor = "kit/tools/pascal/objcheck.py"
reason = "byte-identical output including the implied DGROUP symbol and both padding lines (#33); no flags of its own to lose"



[script."asmverify.py"]
state = "archived"
successor = "kit/tools/pascal/routines.py"
reason = "all 77 rows identical (#33), the lock moved into the register, and --probe ported under #36 and verified to return the identical candidate list [(409,238),(447,200),(470,177)] on segment 1107"


[script."block14b9.py"]
state = "archived"
successor = "kit/tools/pascal/blockcmp.py"
reason = "one of four copies differing only in constants; now blocks/14b9.toml"


[script."block154d.py"]
state = "archived"
successor = "kit/tools/pascal/blockcmp.py"
reason = "one of four copies differing only in constants; now blocks/154d.toml"


[script."block193a.py"]
state = "archived"
successor = "kit/tools/pascal/blockcmp.py"
reason = "one of four copies differing only in constants; now blocks/193a.toml"


[script."block19a0.py"]
state = "archived"
successor = "kit/tools/pascal/blockcmp.py"
reason = "one of four copies differing only in constants; now blocks/19a0.toml"


[script."blockcmp.py"]
state = "carry"
reason = "one segment, block by block; the five blocks*.py scripts merged into it, their segment lists now config"

[script."blocks.py"]
state = "archived"
successor = "kit/tools/pascal/blockcmp.py"
reason = "every row reproduced against real build output (#34); the segment, length, window and block list became a config"


[script."build.py"]
state = "archived"
successor = "kit/tools/pascal/build.py"
reason = "the second consumer's build harness. It donated the compiler-as-a-variable shape, the TP6 dialect rewrite and the SongUnit bootstrap prepass; its data is build.toml. VERIFIED at #35: all 26 unit rows identical. It outlived that by two tickets because probe.py and verify.py imported tp6_dialect from it -- probe.py is now the kit's codegen.py and verify.py imports the kit's copy, so the last hold on it is gone. THE KIT'S OWN build.py CARRIES THIS NAME, which is why archived means 'no ORIGINAL on disk' rather than 'no file of that name'"



[script."build_assets.py"]
state = "decline"
reason = "project driver: builds assets/ from NEUROSIS.DAT"


[script."census.py"]
state = "carry"
reason = "the kit's script-retirement census, and the row now means only that. THE SECOND CONSUMER HAD A COMPLETELY DIFFERENT TOOL UNDER THIS NAME -- four cheap measurements on one segment: far returns as a routine signature, string absence as evidence, far calls out, and CALLF [DI+nn] sites giving the VMT layout. A table keyed by basename cannot hold both, and this row very nearly archived a live tool on the strength of a successor that is an unrelated program. It is kit/tools/pascal/survey.py now"

[script."survey.py"]
state = "archived"
successor = "kit/tools/pascal/survey.py"
reason = "RENAMED from the second consumer's census.py, which collided with the kit's. VERIFIED on four segments -- 17cf, 116e, 1a17 and 12ba -- output identical line for line, 108, 41, 45 and 43 lines. It could not be run as it stood: its image path still pointed at work/unpack/ from when it lived in the other repository, so it crashed on every invocation, and its census row said carry. Its 27-entry segment table, whose own comment asked somebody to keep it in step with verify.py's UNITS, is gone -- the unit config already held it"


[script."coverage.py"]
state = "archived"
successor = "kit/tools/pascal/coverage.py"
reason = "how much of the target is accounted for -- the complement of the per-unit table, because that table says nothing about a segment nobody has started. IT HAD BEEN READING ZERO FOR THE PROGRAM: it shelled out to progcmp.py, archived under #36, and its regex found no match, so `n` fell back to 0 and 1,616 verified bytes dropped out of the total with nothing said. A regex returning None on a MISSING TOOL is indistinguishable from a tool that measured nothing, so it refuses now. Repointed at blockcmp: 99.8% restored, and 12 bytes higher than the figure on record because #45 fixed progcmp's clipping"


[script."datcarve.py"]
state = "decline"
reason = "project driver; but png_indexed() is a dependency-free indexed PNG writer worth carrying separately"


[script."datmap.py"]
state = "decline"
reason = "project driver: NEUROSIS.DAT region map"


[script."dgroup.py"]
state = "archived"
successor = "kit/tools/pascal/dgroup.py"
reason = "compares the initialised DGROUP image. The reference image, the map, the executable and the DGROUP segment number all moved out -- and the segment number turned out to be the same 0x1caa that two other tools carried as a boundary marker. VERIFIED: identical output, the layout agrees for the first 3184 bytes"


[script."dosbuild.py"]
state = "archived"
successor = "kit/tools/pascal/build.py"
reason = "all 35 executables byte-identical (#35); its data is build.toml, generated from it by parsing"


[script."emit_byebye.py"]
state = "archived"
successor = "none -- spent, output committed"
reason = "src/gen/P9IMG.INC states its provenance: read out of bin/NEUROSIS.009 at DGROUP+2, 00C3:0002 in the debug info, named IMAGEDATA, 4,000 bytes -- and it names this emitter"


[script."emit_costab.py"]
state = "archived"
successor = "none -- spent, output committed"
reason = "src/asm/COSTAB.INC states its provenance and names this emitter"


[script."emit_gain.py"]
state = "decline"
reason = "spent one-shot, output committed. Its VALUE is a property, not code: it reassembles and byte-compares before printing, and fails rather than emitting something plausible"


[script."emit_p6shape.py"]
state = "archived"
successor = "none -- spent, output committed"
reason = "src/gen/P6SHAPE.INC states its provenance: NEUROSIS_006_fpu.exe at DS:$000A, 2,048 bytes -- and it names this emitter"


[script."emit_p6text.py"]
state = "archived"
successor = "none -- spent, output committed"
reason = "src/gen/P6TEXT.INC states its provenance: NEUROSIS_006_fpu.exe at DS:$0C8A, 113 lines -- and it names this emitter"


[script."emit_pascal_data.py"]
state = "archived"
successor = "kit/tools/pascal/emit.py"
reason = "the typed-constant emitter. WHAT MOVED is the formatter and the reading; what stayed is every offset, length, type name and line of prose, all of it in emit.toml. VERIFIED: all three generated files' CONST BODIES are byte-identical, 8,010 values -- and a full rebuild leaves all 35 executables byte-identical, which is the test that matters since these are {$I} includes. The HEADERS deliberately changed: each now names the binary AND the offset it came from, which is what #17's provenance gate asks of a generated file before its emitter can be retired. It also confirmed something worth knowing: the bodies are identical although the source is now the trap-rewritten variant rather than the plain file, so that patching leaves this DGROUP data alone"


[script."emit_pascal_data2.py"]
state = "decline"
reason = "second copy of emit_pascal_data's formatter; carry that one instead"


[script."emit.py"]
state = "carry"
reason = "THE KIT'S, born there: the typed-constant formatter with the nested-parenthesis rule, and the reading that feeds it. A multidimensional typed constant needs ((x, y, z), ...) and Turbo Pascal rejects a flat list of the same numbers, which fails a long way from its cause"

[script."encaudit.py"]
state = "archived"
successor = "kit/tools/encaudit.py"
reason = "text I/O that leaves the encoding to the locale. Its DEFAULT_DIRS was a constant naming one repository's two folders -- so the KIT'S OWN SCRIPTS were never audited by it unless somebody passed them by hand, and the folders it named have since been emptied by this migration. Reading encaudit.dirs it now covers 53 files against 19. Both repositories had a copy and now neither does"


[script."fpconst.py"]
state = "archived"
successor = "kit/tools/pascal/x87.py"
reason = "decodes the constants the rewritten code turns out to load. VERIFIED identical on all four formats -- AND IT COULD NOT DECODE ONE OF THEM: its own docstring described the 4-byte operand as 'FADD/FCOMP float ptr', which is what a disassembler prints, while its table keyed it as `single`, so asking for `float` raised KeyError. Both names work now. Its part-to-file lookup was a hardcoded work/split path with a fallback and is target.original"


[script."fpfix.py"]
state = "archived"
successor = "kit/tools/pascal/x87.py"
reason = "rewrites confirmed traps back to real x87. VERIFIED both paths: the PATCHED IMAGE IS BYTE-IDENTICAL on four real trap sites, 4 of 4 applied, and identical again on three offsets that are not traps, 0 of 3 applied with all three named"


[script."fpsurvey.py"]
state = "archived"
successor = "kit/tools/pascal/x87.py"
reason = "surveys the trap sites and is what keeps the encoding table honest -- the table is empirical, from one 1994 binary, so a new target runs this before trusting the rewriter. VERIFIED: identical output on two parts"


[script."glossary.py"]
state = "carry"
reason = "turns the glossary's Avoid lists into a check"


[script."kbprofile.py"]
state = "carry"
reason = "our stricter wiki profile, and the generators for what is generated"


[script."ledger.py"]
state = "archived"
successor = "kit/tools/pascal/markers.py"
reason = "the repaired successor: ledger's DOTALL regex crossed comment boundaries, giving 18 of 317 routines with three rows that were not routines at all"


[script."linkcmp.py"]
state = "archived"
successor = "kit/tools/pascal/linkcmp.py"
reason = "identical line for line, all 31 lines (#33); no flags of its own"


[script."linkorder.py"]
state = "archived"
successor = "kit/tools/pascal/linkorder.py"
reason = "predicts the link order from the uses graph and diffs it against the original's. Its unit-name map, its no-segment set and the original's order all moved into link.toml. VERIFIED: identical output, 30 of 30 positions agree"


[script."mapcmp.py"]
state = "archived"
successor = "kit/tools/pascal/mapcmp.py"
reason = "compares linked segment lengths on the same footing. ITS OUTPUT CHANGED, and both changes are repairs: its copy of the segment list was missing VTSHELL at 0x1931, so that unit was never compared -- and because each length is the NEXT segment's address minus its own, the gap also inflated OBJECTS by exactly VTSHELL's 144 padded bytes and the tool reported it as 'short -- 144 byte(s) of routines nothing references'. That was an artefact of the missing row, not an observation. Now 30 of 30 exact"


[script.'marker.py']
state = "carry"
reason = 'the one reader for @asm markers; three programs had their own and one had already diverged'

[script."markers.py"]
state = "carry"
reason = "reads the @asm markers out of the source tree"


[script."mktests.py"]
state = "decline"
reason = "THE RECORD'S. It generates THIS demo's harnesses and would need a scene inventory in the answers file before it could move -- a known future move, named rather than half-done (#17). Its row said `carry`, which read as work outstanding when the decision was to keep it"


[script."modex.py"]
state = "archived"
successor = "kit/tools/substrate/modex.py"
reason = "de-interleaves four Mode-X planes; unchained VGA, nothing about Pascal. VERIFIED: deinterleave() returns an identical 256,000-byte image from both copies. build_assets.py -- which stays with the record -- was repointed at substrate.modex, because it was the one caller that survives"


[script."mzinfo.py"]
state = "archived"
successor = "kit/tools/substrate/mzinfo.py"
reason = "MZ headers, and whether the file is bigger than its image. VERIFIED: identical output on bin/NEUROSIS.000. Its opening line named this demo and now states the question instead"


[script.'objcheck.py']
state = "carry"
reason = "a {$L} object module against the object's OWN relocations, which is stricter than the zero rule rather than looser; supersedes asmcheck.py"


[script."observe.py"]
state = "carry"
reason = "records a run somebody actually made, and nothing else"


[script."okfcheck.py"]
state = "carry"
reason = "OKF v0.1 conformance only; no project facts, deliberately"


[script."omf.py"]
state = "archived"
successor = "kit/tools/substrate/omf.py"
reason = "the OMF fixup reader. Its only importer was verify.py, so it went with it. VERIFIED: identical code length and identical fixup sets on both object modules in that target -- PLAYMOD 695 bytes with 36 fixups, SOUNDDEV 2,430 with 384"



[script."p2obj.py"]
state = "archived"
successor = "none -- spent, output committed"
reason = "src/gen/P2OBJ.INC states its provenance and names this emitter"


[script."pairmap.py"]
state = "decline"
reason = "the PUSH CS literal-scoring idea is sound and one-way, but it is a project-specific driver; carry the idea if it proves needed"
kept = "KEPT under #36. The disposition says 'NameError on refpath; it cannot run' and THAT IS FALSE -- it runs and prints its table. census.py's own header had already corrected the claim, and the later generated document carried it anyway"


[script."paslint.py"]
state = "archived"
successor = "kit/tools/pascal/paslint.py"
reason = "VERIFIED identical on this repository, 0 problems in 67 files -- AND THE MOVE FIXED A CHECK THAT COULD NOT FAIL. Its source directory was the constant ROOT/'src'; the second consumer keeps its sources under v1.31b/src, so running it there reported '0 problem(s) in 0 file(s)' and passed. Reading layout.src instead, it lints 29 files there. Both repositories had a copy and now neither does"


[script."plan.py"]
state = "carry"
reason = "the ordered queue of investigations"


[script."fingerprint.py"]
state = "archived"
successor = "kit/tools/substrate/fingerprint.py"
reason = "RENAMED from this repository's probe.py, per #17: there were TWO unrelated tools called probe.py, one in each consumer, and both are renamed rather than one -- renaming only one would leave an older document's mention of 'probe' ambiguous for ever. This one answers what is appended past the load image and what wrote it. VERIFIED: identical output on the packed file and on a part with an overlay"

[script."codegen.py"]
state = "archived"
successor = "kit/tools/pascal/codegen.py"
reason = "RENAMED from the second consumer's probe.py -- the other tool of that name, sharing nothing with the fingerprinter. It compiles a probe unit with every installed compiler and diffs the code, which turns a claimed compiler difference from an assertion into a measurement; five of six such claims in that target were wrong. VERIFIED: identical output, all 40 lines. IT COULD NOT RUN AS IT STOOD -- #36 deleted the committed vt131.conf it took from build.py, and the differential needed that file restored from the archive tag to get an honest comparison. Its successor writes the config AFTER wiping the staging directory, which is what the original could not do"




[script."progcmp.py"]
state = "archived"
successor = "kit/tools/pascal/blockcmp.py"
reason = "a blockcmp case rather than a fourth instrument (#45); its headline figure was wrong -- it clipped a block running past the end of our segment and reported 149 real differences for 368 identical bytes"


[script."project.py"]
state = "carry"
reason = "the only reader of a project's answers file; every other program in the kit asks it where things are"

[script."ratchet.py"]
state = "carry"
reason = "the ratchet: coverage may only rise"


[script."refpath.py"]
state = "decline"
reason = "THE RECORD'S: where DemoVT's reference image is, a fact about that checkout. Its row said `carry`"


[script."register.py"]
state = "carry"
reason = "the one writer for status.toml; refuses a section it does not know"


[script."relmatch.py"]
state = "decline"
reason = "THE RECORD'S: how far 1.39b's code matches 1.31's, a fact about that pair of releases rather than about Borland Pascal. Its row said `carry`, and it is the stated exception to the rule that a generic instrument written against one target still moves"


[script."repair_map.py"]
state = "archived"
successor = "none -- spent"
reason = "verified spent under #36: run today it answers '00-map.md looks already repaired (317 blank lines of 1289) -- nothing to do'"


[script."repairdoc.py"]
state = "archived"
successor = "kit/tools/repairdoc.py"
reason = "repairs a markdown file whose line breaks have been multiplied -- a fact about the tools a session is driven with, which every project inherits on day one. VERIFIED: identical report on docs/README.md"


[script.'routines.py']
state = "carry"
reason = 'per-routine byte check, the successor to asmverify.py; its lock lives in the register rather than in a source dict'

[script."rtlfind.py"]
state = "archived"
successor = "kit/tools/pascal/rtl.py"
reason = "names RTL routines by pattern rather than by offset -- the corrected assumption at the heart of this tool, since smart-linking preserves offsets only for a stable core. VERIFIED identical on all nine parts, and the JSON it writes is content-identical. Its per-part RTL bases, its reference part and its thirteen confirmed offsets are rtl.toml. THE MOVE FOUND A GAP IN THE ANSWERS FILE: this tool covered nine parts from its own hardcoded path while target.original listed eight -- 009 was missing, and is there now"


[script."rtlmatch.py"]
state = "archived"
successor = "kit/tools/pascal/rtl.py"
reason = "finds the runtime block in a binary and measures how far it agrees with a reference. VERIFIED: identical output. Its masking and offset arithmetic were already imported by rtlfind, so the two were one tool in two files"


[script."scan_funcs.py"]
state = "archived"
successor = "kit/tools/pascal/rtl.py"
reason = "enumerates likely procedure entry points by prologue shape. ITS OUTPUT CHANGED AND THE CHANGE IS A REPAIR: it carried its own segment-length table, and for three segments the declared length EXCEEDED the space before the next segment begins -- 1000 by 5 bytes, 1931 by 14, 19a0 by 1 -- so those scans ran into the neighbour and attributed 4 entry points to the wrong segment. Deriving each extent from the next segment's base cannot do that: 350 becomes 346. It also no longer scans the DGROUP segment, which is data and had 0 anyway"


[script."segmap.py"]
state = "archived"
successor = "kit/tools/substrate/segmap.py"
reason = "real-mode segment layout derived from the relocation table. VERIFIED: identical 9-line output"


[script."x87.py"]
state = "carry"
reason = "THE KIT'S, born there: the three-step x87 job -- survey the emulator traps, rewrite the confirmed ones, read the constants the rewritten code loads. Three scripts merged into one tool because they are three steps of ONE job, and doing the third without the first is how a wrong theory becomes a table of plausible constants"

[script."wizard.py"]
state = "carry"
reason = "installs the kit into a project: proposes with its evidence, asks what no listing implies, and --check diffs against a kit.toml already there"

[script."rtl.py"]
state = "carry"
reason = "THE KIT'S, born there: the runtime in three steps -- find the block and measure it, name routines inside it by pattern, and enumerate likely procedure starts anywhere by prologue shape. Two of the three already shared code, so they were one tool in two files"

[script."shapediff.py"]
state = "archived"
successor = "kit/tools/pascal/spans.py"
reason = "the coverage walk, and the instrument the top open investigation runs on. IT WAS BROKEN BY THIS MIGRATION: it imported asmverify, archived under #36, so it had stopped running entirely -- the fourth time a deletion here caught a surviving script. What was missing from the kit was not the measurement, which align.spans() has held since #33, but a way to RUN it and an equivalent of --same. VERIFIED against the original restored from the archive tag: parts 001, 002 and 003 identical in every figure including the recorded 66.6% and 49.9%, --same identical, and part 005 aligns 24 bytes MORE -- 80.4% against 80.2%, the SAME 20 spans, two of them starting 14 bytes later and ending 10 earlier. That is the two density gates being formulated differently, measured back to back on a byte-identical TPART5.EXE: not a source change and not a stale figure. The kit's gate is the one #33 measured against all 77 routines. No span is lost, so no work is hidden. It also reads target.release rather than target.original, which is a distinction the answers file did not carry until now"


[script."shared_asm.py"]
state = "carry"
reason = "assembler duplicated between units instead of shared as one include; written here, project exemptions passed in"


[script."spans.py"]
state = "carry"
reason = "THE KIT'S: the coverage walk made runnable, plus --same, which asks whether an unaligned span is code already transcribed in ANOTHER harness -- turning the job from write this into share what exists"

[script."split.py"]
state = "archived"
successor = "kit/tools/substrate/split.py"
reason = "an MZ image plus its appended payload. VERIFIED: identical output splitting bin/NEUROSIS.008"


[script."split_part1.py"]
state = "archived"
successor = "none -- spent"
reason = "verified spent under #36: its input src/PART1_INTRO.PAS no longer exists, so it cannot run. The same split was later done three times without a tool"


[script."staged.py"]
state = "carry"
reason = "is a build output still the source it was built from; text not timestamps, and it says unknown where no copy was staged"

[script."strings.py"]
state = "archived"
successor = "kit/tools/substrate/strings.py"
reason = "printable strings, restricted to the load image. VERIFIED: identical 160-line output"


[script."symbols.py"]
state = "archived"
successor = "kit/tools/substrate/symbols.py"
reason = "the 0x52FB name pools. VERIFIED: identical output"


[script."tddump.py"]
state = "carry"
reason = "Borland debug info, decoded whole"


[script."undeclared.py"]
state = "archived"
successor = "kit/tools/pascal/undeclared.py"
reason = "identifiers a unit uses but never declares. THREE project facts came out of it -- the source directory, which units export the names anything may use without declaring, and which files are worth scanning, the last of which was the glob PART*.PAS and is a naming convention no other target has. VERIFIED: identical output, all 51 lines"


[script.'units.py']
state = "carry"
reason = "each compiled unit's code against the segment it rebuilds, by prefix; supersedes verify.py"


[script."unlzexe.py"]
state = "archived"
successor = "kit/tools/substrate/unlzexe.py"
reason = "LZEXE 0.91. VERIFIED: the unpacked image is BYTE-IDENTICAL, sha256 0177fce7ba1080b1..., from both copies of the unpacker -- the only difference in the run was the output path each was told to write. Both repositories had a copy and now neither does. Its docstring named one file in one demo; it now carries the SHAPE that finding has -- a single packed file in an otherwise unpacked release is often somebody else's code"


[script."vecobj.py"]
state = "archived"
successor = "tools/build_assets.py"
reason = "THE DISPOSITION'S REASON WAS WRONG and the decision was right. It said 'output committed as src/gen/P1VECT.INC'; that file names emit_pascal_data2.py, and vecobj.py never wrote it -- it writes into assets/. Checked instead: build_assets.py carries its own EMBEDDED table producing the same vector_globe and vector_logo_a, assets/ is committed (163 files), and assets/README.md names build_assets.py as the producer with the DGROUP offsets in its table. So it is spent, superseded by a tool that STAYS with the record"


[script."verify.py"]
state = "archived"
successor = "kit/tools/pascal/units.py"
reason = "kept twice under #33 and #36 because units.py reproduced every ROW while dropping its two diagnostic views. Those are ported: align.regions() lists every divergent region and align.hexpair() renders one with a mark line, behind units.py --all and --detail. VERIFIED on a build made to FAIL on purpose (/$R+ turned 0 mismatched into 19): all 566 printed regions identical across all 19 units, per-unit counts identical on every one, and the hex dump identical byte for byte. Two callers repointed first -- coverage.py, whose answer is unchanged at 96.1%, and relmatch.py, which cannot be exercised here because its input tree is held out of source control"



[script."vtbuild.py"]
state = "decline"
reason = "dosbuild.py with renaming removed, and it cannot run without the held-out 1.39b release. #35 confirmed the decline rather than porting it: the release tree is absent on this machine, so a config for it could not be exercised, and an unexercised path in the kit's highest-risk tool is what #49 argued against. Its one unique feature -- rewriting a staged TPC.CFG's /U line -- is recorded here and nowhere else"
