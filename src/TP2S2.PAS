{ ===========================================================================
  Test harness for part 002 scene 2 -- solid 3-D objects, starfield, typed banner.

  Generated by tools/mktests.py -- do not edit; change the generator instead.
  Needs NEUROSIS.DAT in the current directory, so run it from run\.

  Build:  python tools/dosbox/dosbuild.py TP2S2
  =========================================================================== }

program TP2S2;

uses Crt, VGA, P2View, P2Fix, DemoVT, P2S2;

var
  F : file;
begin
  Assign(F, 'neurosis.dat');
  {$I-} Reset(F, 1); {$I+}
  if IOResult <> 0 then
  begin
    WriteLn('TP2S2: neurosis.dat not found in the current directory.');
    WriteLn('       Run this from the run\ folder.');
    Halt(1);
  end;
  Close(F);

  WriteLn('part 002 scene 2 -- solid 3-D objects, starfield, typed banner');
  WriteLn('Press a key to start.');
  ReadKey;

  Scene2;

  Port[$3C8] := 0;
  TextMode(CO80);

  WriteLn('Done.');
end.
