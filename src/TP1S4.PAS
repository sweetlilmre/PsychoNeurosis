{ ===========================================================================
  Test harness for part 001 scene 4 -- the tumbling ball grid and the message.

  Generated by tools/mktests.py -- do not edit; change the generator instead.
  Needs NEUROSIS.DAT in the current directory, so run it from run\.

  Build:  python tools/dosbox/dosbuild.py TP1S4
  =========================================================================== }

program TP1S4;

uses Crt, VGA, DemoVT, P1S4;

var
  F : file;
begin
  Assign(F, 'neurosis.dat');
  {$I-} Reset(F, 1); {$I+}
  if IOResult <> 0 then
  begin
    WriteLn('TP1S4: neurosis.dat not found in the current directory.');
    WriteLn('       Run this from the run\ folder.');
    Halt(1);
  end;
  Close(F);

  WriteLn('part 001 scene 4 -- the tumbling ball grid and the message');
  WriteLn('Press a key to start.');
  ReadKey;

  SetMode13h;
  VirtScrAlloc;
  MusicDetect;          { harmless with no player resident }
  Scene4;

  VirtScrFree;
  SetMode13h;         { 1000:0097 -- what the driver does after scene 3 }
  SetTextMode;

  WriteLn('Done.');
end.
